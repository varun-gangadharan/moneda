// Sign Up / Sign In. Supabase Auth: email+password, magic link, OAuth.
// TODO(Phase 1): implement auth forms + redirect to /onboarding if incomplete,
// else to the demo index. This is a client-side scaffold placeholder.
"use client";

export default function LoginPage() {
  return (
    <main>
      <h1>Sign in</h1>
      <p style={{ color: "var(--muted)" }}>
        TODO(Phase 1): email/password, magic link, and OAuth (Google/GitHub) via
        Supabase Auth. On success, redirect to onboarding if incomplete.
      </p>
    </main>
  );
}
